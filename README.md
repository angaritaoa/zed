# zed
Mis configuraciones personalizadas del editor Zed

mkdir -p ~/.local
tar -xvf <path/to/download>.tar.gz -C ~/.local
mv ~/.local/zed.app ~/.local/zed
ln -sf ~/.local/zed/bin/zed ~/.local/bin/zed
cp -f /mnt/archivos/software/zed/zed.desktop ~/.local/share/applications
